# 自动保存文件（Emacs 格式，以 # 开头的行是注释）
*~
\#*
.\#*

# 自动保存目录
auto-save-list/

# 自定义的配置文件（如果需要备份，请移除这一行）
custom.el

# 包安装目录（elpa 是内置的，elpa-bak 可能是备份）
elpa/
elpa-bak/

# 早期版本可能使用的包目录
vendor/

# 编译后的字节码文件
*.elc

# 项目特定的编译输出（如若有）
build/